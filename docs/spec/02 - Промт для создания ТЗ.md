Роль: Системный архитектор и технический писатель.
Задача: Создать строгое техническое задание (ТЗ) для реализации модуля памяти ИИ-агентов на языке Java 21 — собственной переработки (Gestalt), НЕ порта и НЕ переписывания исходного проекта. Подходы и техники, проверенные в аналогах (в т.ч. Honcho), заимствуются только как идеи; прямое копирование чужого кода, внутренних имён DTO, текстов промптов и закрытых структур исключено (Р1, Р15, Р17). Внешние адаптеры данных (data import) — чтение открытых форматов экспорта в собственную модель.

Входные данные (вставь сюда: STATUS.md — реестр решений и открытые нити; карточки алгоритмов; концептуальные доки docs/concept):
[ССЫЛКИ НА МАТЕРИАЛЫ GESTALT]

Требования к структуре ТЗ:
1. Концептуальная модель: Описание сущностей (Психологический профиль, Векторное воспоминание, Графовая связь) без привязки к конкретным библиотекам.
2. Алгоритмы обработки (Step-by-Step): Описать пошагово в виде псевдокода или Enterprise-диаграмм процессы (функциональные роли; имена воркеров — по нити Н1):
   - Как экстрактор извлекает сырые факты.
   - Как запросный уровень формирует противоречия/выводы.
   - Как консолидатор выполняет асинхронную консолидацию памяти (сжатие).
   - Как индуктор (новый уровень) формирует паттерны поведения и психологический портрет.
3. Матрица разграничения данных: Разделение реляционного графа (таблицы рёбер + recursive CTE) и векторов (pgvector); графовый движок — за SPI (Р7).
4. Требования к интерфейсам (SPI): Описание контрактов для плагинов (чтобы можно было написать стартер для Spring/Micronaut).

Инструкция: Избегай копирования названий внутренних переменных, имён DTO и текстов промптов исходных проектов, специфичных для Python конструкций. Используй паттерны ООП, concurrency-модели Java и абстрактные типы данных.

