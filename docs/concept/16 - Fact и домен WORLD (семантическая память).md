# Gestalt — 16: Fact и домен WORLD (семантическая память)

> 2026-09-05 · Источник: концептуализация семантической памяти · Статус: **канон** (доменная симметрия с `09`; принят в развитие **Р5, Р6, Р7, Р10, Р13, Р19, Р23, Р29, Р30**; STATUS).
> Дополняет: `06 - Концептуальный фундамент.md` (§3 Q1: онтология содержания), `09 - Construct.md` (симметрия доменов PSYCHE vs WORLD), `10 - Проекты и радиус применимости.md` (контейнеры и scope), `13 - Таксономия процессов памяти.md` (конвейер памяти), `14 - PortraitSnapshot.md` (секция CRITICAL), `17 - Динамика памяти (доступность, затухание, устойчивость).md` (затухание, доступность истории).

---

## 1. Назначение и концептуальное место

В концептуальном фундаменте Gestalt память разделена по таксономии Тульвинга на три ортогональных слоя:
1. **Эпизодика (`Message`):** сырые логи диалогов («что и когда было сказано»).
2. **Семантика мира (`Fact/WORLD`):** точные наблюдаемые данные о мире, включая пользователя как физический объект мира («что существует и истинно в мире»).
3. **Модель человека (`Fact/PSYCHE` + `Construct`):** интенциональная ToM-модель профессионального почерка и смысловых осей инженера («как и почему он мыслит»).

Если домен `PSYCHE` опирается на теорию личностных конструктов Келли (`09 - Construct.md`), то домен `WORLD` — это **классическая семантическая инженерия знаний (Knowledge Representation)** с би-темпоральностью, строгим скоупингом и гибридным извлечением.

Факты мира («проект на Java 21 LTS», «порт базы 5432», «отказались от JPA в пользу jOOQ») принципиально **не являются конструктами Келли**:
- У них нет биполярных полюсов («Java 21 vs что?» — это абсурд).
- Они не отражают внутреннюю шкалу ценностей человека, а фиксируют внешнее состояние среды.
- Однако они служат **условиями (`conditions`)** для конструктов и сырьём для индукции ToM-портрета.

Настоящий документ определяет, как хранить точные данные мира, как управлять их жизненным циклом при накоплении тысяч записей (anti-mess) и как доставлять их в контекст агента без его переполнения.

---

## 2. Онтология и модель сущности `Fact`

Каждый факт в системе однозначно позиционируется в трёхмерном пространстве (**Р13**):
$$\text{Fact} = \langle\text{owner}, \text{domain}, \text{scope}\rangle$$

- `owner` — пользователь (Actor `PERSON`). Бесхозных фактов не бывает.
- `domain = WORLD` — онтологическая категория (наблюдаемое о мире).
- `scope ∈ {SESSION, PROJECT, USER}` — радиус релевантности внутри владельца (`10 §3.1`).

### 2.1 Схема сущности `Fact`

```
Fact {
  // 1. Идентификация и адресация
  id: UUIDv7,                           // Time-ordered первичный ключ
  owner_id: UUID,                       // Владелец (User)
  observer_id: UUID,                    // Наблюдатель (Actor, Р3): чья перспектива;
                                         // v1 — всегда агент сессии; фильтрация перспективы —
                                         // query-параметр. observed в Fact не хранится:
                                         // в AU observed ≡ owner (у ConstructInstance — есть)
  domain: WORLD,                        // Доменная категория (Р13)
  scope: SESSION | PROJECT | USER,      // Радиус релевантности (Р13)
  project_id: UUID?,                    // Nullable: привязка к проекту (10 §3.1)

  // 2. Семантическое содержание
  statement: String,                    // Человекочитаемое утверждение для LLM и векторизации
                                         // («Проект gestalt разрабатывается на Java 21 LTS»)
  kind: STATE | NARRATIVE | EVENT,      // Вид факта — природа содержания; задаёт
                                         // обязательность полей anchor (§2.2)
  anchor: {                             // Структурированный якорь (EAV-триплет)
    subject: String,                    // «gestalt» — обязателен для всех видов
    predicate: String?,                 // «runtime.version» — обязателен только для STATE
    object: String?                     // «Java 21 LTS» — обязателен только для STATE
  },
  conditions: [String],                 // Условия применимости значения («dev», «prod»); дефолт ∅;
                                         // входит в identity (§2.3): разные conditions — sibling, не supersession

  // 3. Би-темпоральность (Снодграсс, Р5; SQL:2011)
  valid_time: [Timestamp, Timestamp?),  // Интервал истинности в мире (t_end = null -> актуален)
  system_time: Timestamp,               // Время фиксации в Gestalt

  // 4. Статус и история (SCD Type 2, Р6)
  status: ACTIVE | SUPERSEDED | RETIRED,
  superseded_by_id: UUID?,              // Ссылка на факт-преемник
  generalized_from: [UUID],             // Сводный факт → атомы: провенанс Generalization
                                         // (симметрия superseded_by_id; 17 §6, Р29)

  // 5. Эпистемический вес и провенанс
  confidence: Float,                    // Доверие [0.0 .. 1.0]
  reinforcement_count: Int,             // Счётчик независимых подтверждений (Р10)
  last_seen_at: Timestamp,              // Время последнего упоминания / подкрепления
  lineage: [UUID],                      // Ссылки на исходные Message.id

  // 6. Политики приоритизации
  is_critical: Boolean,                 // Маркер инварианта -> попадает в PortraitSnapshot.CRITICAL
  permanence: INFERRED | USER_ASSERTED  // Явное «запомни навсегда» от юзера -> защита от TTL
}
```

### 2.2. Виды фактов и обязательность anchor

Два независимых измерения факта, которые не следует путать:

- **anchor** (§2.1) — *форма записи*: триплет `subject → predicate → object`, адресуемая часть факта;
- **kind** — *природа содержания*: каким по сути является факт. Вид определяет, насколько полным
  обязан быть anchor; anchor не определяет вид.

| Вид | Природа | Примеры | Обязательность anchor |
|---|---|---|---|
| **STATE** | состояние/свойство мира: значение, версия, порт, путь, зависимость, имя, конфигурация | «проект на Java 21 LTS», «порт базы 5432», «зависимость jOOQ» | полный: subject + predicate + object — **обязателен** |
| **NARRATIVE** | решение, договорённость, причина, контекст выбора | «отказались от JPA в пользу jOOQ из-за N+1» | частичный: subject [+ predicate]; смысл живёт в `statement` |
| **EVENT** | инцидент, происшествие, эпизод мира | «хост упал от OOM в 3 ночи» | вырожденный: subject; identity — `valid_time` |

Правила:

1. **Полный anchor обязан только STATE.** Принуждение экстрактора триплетировать всё порождает
   выдуманные предикаты для NARRATIVE/EVENT (predicate sprawl — аналог axis sprawl, Н4) и разрушает
   дедуп/supersession по `(subject, predicate)`.
2. **Exact-дедуп (§4.1) применяется только к фактам с полным anchor** (STATE); NARRATIVE/EVENT
   дедуплицируются семантической веткой (косинус).
3. Вид проставляет экстрактор в том же батч-вызове Encoding (Р6): один enum-токен на факт —
   отдельных вызовов LLM и заметного расхода токенов вид не добавляет.
   4. **Предикаты STATE — эмерджентный словарь с write-time feedback (словарь кладётся в промпт).**
      Словарь рождается пустым у каждого проекта; поставляемых словарей в продукте нет — доменная
      нейтральность (механика едина для инженера, медика и оператора зернодробилки). Extraction-prompt
      несёт живой словарь проекта — **предикаты, subject-имена и conditions-теги** (все три входят
      в identity, §2.3): мягкое предпочтение существующих («повторяй, если предметно подходит;
      новое — только для нового свойства») + naming-политика формы имени (детерминированный
      regex-валидатор) — профилактика фиксации первого неудачного имени как единственного.
      Отбор — **top-N по использованию + «ясли»**: гарантированные слоты для ещё не подтверждённых
      новых имён (иначе rich-get-richer: новорождённое имя не попадёт в top-N и будет продублировано
      синонимом). «Живое» имя — при наличии ≥1 ACTIVE-факта со ссылкой. Словарь — не хранимая
      сущность, а проекция над `facts` (агрегат), и он насыщается: выходит на плато в десятки–сотни
      имён на проект (эмпирика живой базы), промпт не растёт бесконечно. Дрейф синонимов даёт
      split identity; лечение — консолидация (§2.3), а не «пересборка словарей».
5. **Канонизаторы форм `object`.** Нормализация значения — детерминированные pure functions,
   0 LLM; применяются только при сравнении `object` внутри совпавшего identity (§4.1) —
   subject/predicate уже равны, контекст защищает. Состав v1: **количество** (число + опц.
   единица: «8 GB» ≡ «8GB»; единицы не сводим — KB/KiB доменное), **версия** (числовой кортеж,
   хвостовые нули долой, квалификатор-класс: LTS/GA → ∅, alpha/RC → pre — «21 LTS» ≡ «21»),
   **путь** (resolve `.`/`..`, трейлинг-`/` долой; регистр сохраняем — Unix case-sensitive),
   **url** (lowercase scheme+host, дефолтный порт и трейлинг-`/` долой; path/query не трогаем —
   case-sensitive по RFC), **строка** (NFC + trim + collapse пробелов; регистр сохраняем).
   Выбор — каскад по форме значения, специфичнее — раньше: url → путь → версия → количество →
   строка (default); версия и количество сравнимы по числовому ядру (единый тип-класс), иначе
   «21» и «21 LTS» разъедутся по типам. **Принцип консервативности:** канонизатор сливает
   только заведомо равное — он безапелляционен; сомнительная близость (регистр, диакритика,
   единицы) — территория кандидатов консолидации (§2.3), где есть evidence и обратимость.

### 2.3. Identity факта и условия применимости (**Р23**)

Тождественность и значение — разные вопросы («это тот же факт?» ≠ «это то же значение?») и
разводятся явно:

- **identity** — адрес свойства:
  `(owner_id, scope, project_id, subject_norm, predicate_norm, conditions)`.
  Существует только у **STATE** — единственный вид с полным anchor (§2.2).
  Scope-контейнер входит в identity: та же ячейка — только внутри одного контейнера видимости
  (`port` у subject «база» проекта A и проекта B — два факта, не supersession).
- **object** — версииемое значение; в identity **не входит**. Смена значения — событие
  жизненного цикла (supersession, §4.2), а не потеря тождественности. Повторная встреча того же
  значения — подтверждение пропозиции (reinforcement, ACT-R base-level activation).
- **conditions** — условия применимости значения. Один identity, разные значения →
  supersession-кандидат (смена во времени); разные conditions → **sibling** — сосуществующие
  истины («порт базы: 5432 [dev], 5433 [prod]»). Прототип — MV-register (CRDT): конкурентные
  значения сосуществуют до разрешения. Параллель с `conditions` конструктов — 09 §4.

**Инвариант целостности: ≤1 ACTIVE-факт на identity.** Функциональная зависимость
(реляционная теория): у свойства не более одного актуального значения в рамках identity.
Реализация — частичный unique-индекс (служебный, полный — §3.1); побочно ловит гонки
параллельных батчей Encoding.

Нормализация `subject` — минимальная (trim, регистр, пробелы); алиасы («PG» ≡ «PostgreSQL»)
лечит консолидация (ниже). Источники (Р22): пропозициональная идентичность — Кинч и ван Дейк
(1978); функциональная зависимость — Кодд; reinforcement — ACT-R; sibling — MV-register
(Шапиро, 2011).

**Консолидация словарей — общесистемный механизм (один на четыре пространства имён).**
Словари системы: оси конструктов (PSYCHE, per-user), предикаты WORLD, subject-имена,
conditions-теги (per-project). Все — эмерджентные, без поставляемых составов; конвергенция —
write-time feedback (§2.2 п.4); словари — проекции над `facts`, «пересборки словарей» не
существует.

- **Кандидаты склейки** рождаются попутно на write-time при промахе дедупа — сигнальный
  каскад средствами PG (кандидат = любой сигнал ≥ своего порога): (1) `pg_trgm` — литеральная
  близость имён (`stack.db` ~ `stack.database`); (2) **fingerprint ко-использования** — два
  имени претендуют на одну ячейку (общий `subject_norm` + близкий `object`, сходство Жаккара
  по множеству пар) — сильнейший сигнал, чистый SQL; (3) косинус эмбеддингов (pgvector) —
  ловит синонимию без лексического пересечения (`db.host` ~ `database.endpoint`); дефолт 0.85
  (верифицирован внутренним аудитом живой базы: −53% дублей против −22% у 0.90; привязан к
  embedding-модели — конфиг). Это порог **генерации кандидатов** (false positive дёшев — решает
  judge), а не финальной идентификации — ср. строгий 0.90 у NARRATIVE-дедупа (§4.1), где решение
  принимается без судьи. Далее — детерминированная кластеризация (0 LLM) → LLM-judge
  батчем → merge-миграция. LLM здесь — только судья над редкими парами.
- **Регламент judge-батча:** исполнитель — контур **Reconciliation** (техническая гигиена, не
  Dreaming — консолидация словарей не психологический процесс); лимиты — и по времени, и по
  объёму батча; остаток очереди → **drain-режим** (backoff сжимается до исчерпания очереди).
  Вход судьи — пара + мини-конкорданс (2–3 примера фактов с каждой стороны: голые токены
  неосуждаемы). Каноническое имя при merge — детерминированный тай-брейк: использование →
  возраст → лексикография.
- **Merge-миграция:** UPDATE + пере-identity; столкновение двух ACTIVE на новом identity
  разрешается обычным дедупом §4.1 (значение равно → reinforcement; отличается → кандидат
  supersession) — не отдельный механизм.
- **Идемпотентность и провенанс:** одна таблица `dictionary_pairs(namespace, name_a, name_b,
  state, verdict, evidence, judged_at)` с UNIQUE(namespace, name_a, name_b) — повторный
  кандидат гасится ON CONFLICT, вердикты и история merge живут там же; единственная новая
  сущность механизма. Poison: 3 фейла judge → карантин пары (Р10).
- Тот же механизм закрывает merge-порог осей в Н4. Числа (пороги сигналов, размеры батчей,
  каденции) — дефолты конфига → ТЗ (Н8).

---

## 3. Физическое хранение: Pure PostgreSQL 16+ (**Р7**)

Хранилище домена `WORLD` строго консолидировано в единой PostgreSQL-базе без привлечения внешних специализированных графовых или векторных СУБД.

```
                  ┌────────────────────────────────────────────────────────┐
                  │                 PostgreSQL 16+ (Р7)                    │
                  ├────────────────────────────────────────────────────────┤
                  │  Таблица: `facts`                                      │
                  │  ├── B-Tree: (owner_id, domain, scope, project_id)     │
                  │  ├── JSONB / GIN: anchor (subject, predicate, object)  │
                  │  ├── pgvector (HNSW): embedding (statement, 1536d)     │
                  │  └── tsvector / GIN: fts_tokens (lexemes statement)    │
                  └────────────────────────────────────────────────────────┘
```

1. **Таблица `facts`:** хранит все атрибуты записи.
2. **Точные термины (`tsvector` + GIN):** полнотекстовый индекс с русской и английской лемматизацией. Критически важен для инженерного домена: поиск точных версий (`Java 21`), названий библиотек (`HikariCP`, `jOOQ`), номеров портов (`5432`) и параметров конфигурации. Векторные модели часто «размывают» такие токены в абстрактные понятия; FTS даёт 100% точное попадание.
3. **Семантика (`pgvector` + HNSW):** векторное представление текстового поля `statement` для неточных смысловых запросов («как у нас устроена работа с базой данных?» $\to$ подъём факта про Postgres и пул соединений).
4. **Структурные предикаты (JSONB + GIN):** быстрый индекс по полям `anchor`, позволяющий детерминированно находить существующие факты по объекту и предикату без вызова LLM.

### 3.1. Инварианты горячего набора (анти-деградация)

Память растёт годами — поиск деградировать не должен. Два инварианта хранения:

1. **Поисковые индексы сигналов — только ACTIVE** (partial indexes `WHERE status = 'ACTIVE'`):
   HNSW (`embedding`) и GIN (`fts_tokens`) не индексируют историю SUPERSEDED/RETIRED —
   размер горячего набора ограничен живыми фактами независимо от накопленной истории.
   Исключение — GIN (`anchor`): **полный** — exact-адресация истории по subject/predicate
   (адресный доступ без семантического поиска; 17 §6, Р29).
   Служебные B-Tree (адресация, scope-фильтры) — полные.
2. **Reinforcement — UPDATE строки, а не INSERT** (§4.1): подтверждение уже известного факта
   инкрементит счётчик и не трогает тяжёлые индексы — повторы не разбухают HNSW/GIN.

Количественные пороги (p99, бюджеты индексов, параметры HNSW) — NFR, имплементационные детали → ТЗ (Н8).

---

## 4. Жизненный цикл и гигиена: борьба с хаосом (Anti-Mess)

Когда в системе накапливаются тысячи фактов, неконтролируемый рост предотвращается на каждом этапе конвейера памяти:

### 4.1. Fast Path (Encoding): двухшаговый дедуп по identity (**Р6, Р10, Р23**)
- **Проблема аналогов:** создавать строку на каждое упоминание или гонять LLM для слияния (Mem0/Honcho); сравнение триплета целиком («Java 21» ≠ «Java 21 LTS») не отличает дубль от смены значения.
- **Решение Gestalt** — двухшаговый детерминированный алгоритм (STATE; identity — §2.3):
  1. **Identity-lookup:** кандидат ищется по identity среди ACTIVE; по инварианту §2.3 их ≤1.
   2. **Сравнение значения** — нормализованный `object` (канонизатор по форме значения — §2.2 п.4):
     - равен → новый факт **не создаётся**: `reinforcement_count += 1`, `last_seen_at = now`, `lineage += message_id`;
     - отличается → не дубль, смена значения → кандидат в supersession-цепочку (решение за Dreaming, §4.2);
     - identity не найден → новый факт.
   3. **NARRATIVE/EVENT** (полного anchor нет) — семантическая ветка: косинус `statement` ≥ 0.90 —
      финальное решение о тождественности **без судьи**, поэтому порог строже каскадного 0.85 (§2.3),
      где порог лишь генерирует кандидатов на склейку (false positive дёшев — остановит judge).
  4. **`custom`-предикаты** — та же механика; дрейф синонимов даёт split identity, склейка — Dreaming.
  5. Затраты LLM: **0** на этапе дедупликации.

### 4.2. Slow Path (Dreaming): Замещение и версионирование (**Р6, SCD Type 2**)
- **Проблема аналогов:** деструктивный `UPDATE`/`DELETE` затирает историю (Mem0), либо штраф на старые факты ломает контекст рассуждений (HippoGraph).
- **Решение Gestalt:**
  - При смене факта (например, миграция: *«Перешли с Java 21 на Java 25»*):
    1. У старого факта закрывается интервал: `valid_time.t_end = now()`.
    2. Статус старого факта меняется: `status = SUPERSEDED`, `superseded_by_id = new_fact.id`.
    3. Создаётся новый факт со статусом `ACTIVE` и `valid_time.t_start = now()`.
   - **Инвариант:** Старый факт остаётся в БД. Он исключается из базового скоупа актуальности, но доступен для исторического retrieval (*«на чём мы писали до марта 2026?»*). Механизм — три адресных пути (exact по `anchor`, навигация по `superseded_by_id`/`generalized_from`, temporal query-side): **17 §6, Р29**.

### 4.3. Фоновое обобщение (Generalization во «сне», **13 §2**)
- Россыпь однородных микро-фактов (*«добавлен AssertJ»*, *«настроен Testcontainers»*, *«используется JUnit 5»*) в ходе процесса Dreaming кластеризуется и синтезируется в сводный факт уровня архитектуры:
  $$\text{Statement: «Стек модульного и интеграционного тестирования: JUnit 5, AssertJ, Testcontainers»}$$
- Сводный факт несёт провенанс: `generalized_from` — ссылки на атомы (навигация «сводный → атомы», 17 §6, Р29).
- Первичные атомарные факты получают статус `RETIRED` (или понижаются в приоритете), предотвращая засорение контекста фрагментами.

### 4.4. Затухание без стирания: единая кривая затухания (**Р27**, канон **17 §5**)
- Факты не удаляются физически (деструктивное забывание отвергнуто каноном, `06 §8.1`); исторические статусы доступны адресно (§4.2, 17 §6).
- Коэффициент свежести $w$ — **единая кривая затухания в опытном времени** с несгораемым минимумом; доменные параметры $H$/$w_{floor}$ (WORLD-инстанциация), формула и обоснование — **17 §5** (дефолты — конфиг → ТЗ, Н8).
- $w$ влияет **только на ранжирование при поиске** (ranking bias, §5.2) и селекцию в `PortraitSnapshot` — но не стирает данные из базы.
- **Иммунитет от затухания:** факты с `permanence = USER_ASSERTED` или `is_critical = true` имеют $w \equiv 1.0$ всегда.

---

## 5. Двухуровневая модель доставки в контекст

Мы никогда не подсовываем «1000 фактов» в диалог. Поставка знаний разделена на два непересекающихся контура:

**Принцип вычислимой важности (Р30):** важность — вычислимая функция read-path, не атрибут факта; хранимое на следе — только политики доставки и жизни (`is_critical`, `permanence`). Антипаттерн — stored-priority (`12 §12.1`, 17 §3.4).

```
┌────────────────────────────────────────────────────────────────────────┐
│ КОНТУР 1. СТАРТ СЕССИИ (Proactive Base, O(1), 0 LLM)                   │
│                                                                        │
│ PortraitSnapshot (лимит: ≤40 строк, ~2-4k символов)                    │
│ ┌────────────────────────────────────────────────────────────────────┐ │
│ │ [CRITICAL]    Жесткие инварианты мира (Java 21 LTS, Pure Postgres) │ │
│ ├────────────────────────────────────────────────────────────────────┤ │
│ │ [CONSTRUCTS]  Активные оси Келли (почерк инженера: PSYCHE)         │ │
│ ├────────────────────────────────────────────────────────────────────┤ │
│ │ [PREFERENCES] Стабильные предпочтения                              │ │
│ └────────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ КОНТУР 2. ХОД ДИАЛОГА (Reactive Recall, On-demand, по запросу)         │
│                                                                        │
│ WORLD-Recall (лимит: Top 3-5 фактов, ≤500 токенов)                     │
│ ├── 1. Проектный барьер: WHERE project_id = :curr OR scope = 'USER'   │
│ ├── 2. Мультисигнальный поиск: RRF(pgvector, tsvector)                 │
│ ├── 3. Ранжирование: Score = Sim × Decay × log(Reinforcement)          │
│ └── 4. Инвариант No-False-Memory (Р14): тишина, если Score < Порог     │
└────────────────────────────────────────────────────────────────────────┘
```

### 5.1 Контур 1: Проактивный старт сессии (`PortraitSnapshot`, **Р19**)
- **Латентность:** $O(1)$, ~2 мс (готовый текстовый блок, материализованный в PG фоновым Reconciler).
- **Секция CRITICAL:** санкционированное `WORLD`-вкрапление (`14 §2`).
  - В секцию отбираются факты `WORLD`, удовлетворяющие предикату:
    $$\text{status} = \text{ACTIVE} \;\land\; \text{is\_critical} = \text{true} \;\land\; (\text{project\_id} = S.\text{project} \;\lor\; \text{scope} = \text{USER})$$
  - Примеры: архитектурные табу, выбранный стек, корневые ограничения (*«Java 21 LTS, Pure Postgres 16, Spring Boot запрещён»*).
  - **Бюджет:** не более 5–10 строк. Второстепенные факты сюда **не имеют права попасть**.

### 5.2 Контур 2: Реактивный `WORLD-Recall` во время общения в чате (**Н5**)
Срабатывает per-turn только при наличии предметного вопроса пользователя.

1. **Проектная изоляция (10 §3.3):**
   Запрос видит только факты текущего проекта и глобальные факты пользователя:
   ```sql
   WHERE status = 'ACTIVE' 
     AND owner_id = :user_id 
     AND (project_id = :current_project_id OR scope = 'USER')
   ```
   Факты соседних проектов (например, Java 11 из `legacy-service`) физически отсекаются на уровне SQL-фильтра.
2. **Мультисигнальный поиск (Multi-signal retrieval):**
   Объединение результатов семантического векторного поиска и точного текстового поиска через Reciprocal Rank Fusion (RRF):
   $$\text{RRF\_Score}(d) = \frac{1}{k + \text{rank}_{dense}(d)} + \frac{1}{k + \text{rank}_{sparse}(d)}$$
3. **Финальный ранг с учётом затухания и надёжности:**
   $$\text{Final\_Score}(d) = \text{RRF\_Score}(d) \times w(d) \times \left(1 + 0.2 \cdot \ln(1 + \text{reinforcement\_count})\right)$$
   где $w(d)$ — вес свежести по единой кривой затухания (§4.4, 17 §5).
4. **Строгий кап (Top-K budget):**
   В контекст передаётся строго **Top 3–5 фактов** (не более 300–500 токенов).
5. **Инвариант No-False-Memory (Р14):**
   Если максимальный `Final_Score` кандидатов ниже установленного порога отсечения $T_{\text{relevance}}$, память возвращает **пустой список**. Агент не должен галлюцинировать или притягивать факты не по теме (урок NGT/DRM-парадигмы: лучше промолчать, чем вставить неуместный факт).

---

## 6. Сопряжение доменов: как `WORLD` связан с `PSYCHE`

Между семантической памятью (`WORLD`) и моделью личности Келли (`PSYCHE`) действует строгий **инвариант чистоты и «одна дверь»** (`10 §4`):

```
                      ┌────────────────────────┐
                      │      Fact / WORLD      │
                      │ (Java 21, Linux, jOOQ) │
                      └───────────┬────────────┘
                                  │
                 ┌────────────────┴────────────────┐
                 │                                 │
     [Как объект в conditions]         [Сырьё для индукции]
                 │                                 │
                 ▼                                 ▼
      ConstructInstance.conditions        Gestalting (медленный сон)
    «Новизна [в pet-проектах]»                     │
    «Стабильность [в enterprise]»                  ▼
                 │                       Вывод мета-концепта:
                 │                       «Широкий кругозор: ведёт
                 │                       legacy и modern параллельно»
                 ▼                                 │
    ┌─────────────────────────┐                    ▼
    │     PortraitSnapshot    │◄───────────────────┘
    └─────────────────────────┘
```

1. **Инвариант чистоты:** факт `WORLD` напрямую никогда не становится осью или инстансом конструкта личности.
2. **Использование в `conditions` конструктов:** факты мира служат контекстными ограничителями для осей Келли (`09 §3`). Инженер не «вообще консервативен» — он консервативен применительно к `legacy-service` (`conditions: legacy, production`) и радикально открыт новому в личных разработках (`conditions: pet-project, experimental`).
3. **Кросс-проектная индукция во сне (`Gestalting`, 13 §2):**
   Совокупность разнородных проектных фактов (ведёт сервис на Java 11 и проект на Java 21) во время `Gestalting` индуцирует личностное мета-знание о высокой профессиональной гибкости и переключаемости стилей инженера.

---

## 7. Открытые вопросы

Проработка велась по одному пункту за сессию в драфтах (нить **Н12**, STATUS — ✅ закрыта 2026-09-06).

1. **Identity факта и dedup-key:** ✅ решено (2026-09-05) → **Р23**: identity без `object`, двухшаговый дедуп, `conditions`/sibling. Канон §2.3, §4.1.
2. **Словарь предикатов anchor:** ✅ решено полностью (2026-09-05, включая остаток) → **Р25**: feedback-промпт — словарь кладётся (предикаты + subject-имена + теги; top-N по использованию + «ясли»; мягкое предпочтение + naming-политика; §2.2 п.4); канонизаторы v1 — количество/версия/путь/url/строка, каскад по форме, принцип консервативности (§2.2 п.5); сигнальный каскад кандидатов trigram → fingerprint → косинус 0.85 и регламент judge-батча — контур Reconciliation, drain-режим, `dictionary_pairs` (§2.3). Числа — дефолты конфига → ТЗ (Н8).
3. **Доступность RETIRED-истории после Generalization (§4.3):** ✅ решено (2026-09-06) → **Р29** — три адресных пути, `generalized_from` (§2.1), полный GIN(`anchor`) (§3.1), ссылки §4.2/§4.3; канон 17 §6.
4. **Принцип вычислимой важности (§5):** ✅ решено (2026-09-06) → **Р30** — строка-принцип в §5; канон 17 §3.4.

## 8. Связанные документы и решения

- `06 - Концептуальный фундамент.md` — разделение эпизодики, семантики и ToM; словарь (§8).
- `09 - Construct.md` — ядро домена `PSYCHE` (биполярные оси Келли, парный документ).
- `10 - Проекты и радиус применимости.md` — контейнеры, иерархия скоупов `SESSION/PROJECT/USER`.
- `13 - Таксономия процессов памяти.md` — конвейер `Encoding -> Dreaming -> Gestalting`.
- `14 - PortraitSnapshot.md` — материализованный срез профиля, секция CRITICAL.
- `../../STATUS.md` — **Р5, Р6, Р7, Р10, Р13, Р19, Р23, Р29, Р30**; закрытие частей нитей **Н5** и **Н10**; нить **Н12** — ✅ закрыта (2026-09-06).
- `17 - Динамика памяти (доступность, затухание, устойчивость).md` — парный канон динамики: опытное время, единая кривая затухания (§4.4), интерференционные пулы, доступность истории (§4.2/§4.3), вычислимая важность (§5).
