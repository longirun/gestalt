# 26 — Живой тезис-чек (E6, project-v3)

> 2026-09-19 · Статус: **тезис-чек E6 — первый живой прогон каркаса Р45 (smoke-ярус: 28 точек, 0 судей): ассерт `lift > 0` не выполнен (−4 pp, p = 1.0) → условный no-go (спека 28 §4.2), звенья диагностированы (§4), правки предложены до следующего рана (§5). После гигиены датасета (25 точек, §2.1): lift +0 pp, both-fail 17 → 11; правка 3 (датасет) выполнена, правки 1–2 — впереди**. Канон каркаса — `docs/spec/eval/28` (Р45), протокол разметки — `docs/spec/eval/33` (Р45), калибровка прибора (positive control) — `docs/research/27`.

---

## 1. Контекст и цель

Первый живой прогон eval-каркаса: инъекция PortraitSnapshot в плечо A против контроля B на живой разметке (не PC): проверка ассерта тезис-чека — **lift > 0** на детерминированных оракулах (Р36, без судей — спека 28).

* **Материал:** живой лог opencode-сессии project (`per-directory-opencode-project-opencode`), 1538 реплик (срез 2026-07-28…08-11), 28 точек (20 L1 + 8 C) по протоколу 33, окно N = 5.
* **Эксперимент `project-v3`** (ingest_fp `289176d03d03`): после фикс-сессии каркаса (reasoning_effort в fp, point_fp — миграции 007/008, purge ответов при rewrite) и фикса парсера экстрактора (`cleanCodeFences`). `project-v2` архивирован: его слепки построены битым парсером и ранами с падёжами (#136–138) — недоверены, out/-слепки вычищены до рана.
* **Прибор/объект:** одна модель (`local1` через litellm) на обе роли; reasoning-effort не задан.

## 2. Замеры

Стадии (runs #220–224, все done): replay — 34.4 мин, 187 LLM-вызовов, 550 788 prompt / 134 589 completion; фактов 1615, слепков 48 (28 M + 20 W0), 0 skipped. WCheck — 20 W0 проверено, 5 answer-covered, 0 insufficient-grid. Arms — 28 пар (56 вызовов, 744 713 prompt / 9 248 completion); плечо A 19–33K токенов на вопрос (полный слепок), B 1–2K.

| метрика | значение |
|---|---|
| точек | 28 |
| pass A (память) | 9 (32 %) |
| pass B (контроль) | 10 (36 %) |
| **lift** | **−4 pp** |
| только A / только B | 1 / 2 |
| оба pass / оба fail | 8 / 17 |
| C-leak (veto) | 0 |
| МакНемар exact | p = 1.0 (рассогласованных пар 3) |

Против PC (док 27: +48 pp, p ≈ 2·10⁻⁹) на живом материале инъекция слепка прироста не дала; veto-утечек нет.

### 2.1. Пересчёт после гигиены датасета (25 точек)

Правка 3 (§5) выполнена 19.09: выбраковано 7 точек (6 неисполнимых + L1-14404), добавлены 4 новых с гарантированными факт-иглами (L1-12873 `OptimisticLockingFailureException`, L1-12953 `WebApiAuthorizationFilter`, L1-14975 `findForCatalog`, L1-16130 `EclipseLink`; все — И2+И4), перемаркированы 3 парафраза (L1-14925 → `jmix`, L1-16116 → `jpql`, L1-16256 → `produces`). Слепки новых точек построены временной проекцией фактов на границу (`TemporalProjection.atBound`, runs #226–229: 0 LLM на replay, 8 на arms); сироты из PG вычищены, отчёт фильтруется по датасету.

| метрика | до гигиены (28) | после (25) |
|---|---|---|
| pass A / pass B | 9 (32 %) / 10 (36 %) | **11 (44 %) / 11 (44 %)** |
| lift | −4 pp | **+0 pp** |
| только A / только B | 1 / 2 | 3 / 3 |
| оба pass / оба fail | 8 / 17 | 8 / **11** |
| МакНемар exact | p = 1.0 (3 пары) | p = 1.0 (6 пар) |

Рассогласованные пары: только-A — **L1-12873, L1-16130 (обе новые точки с факт-иглами)** и L1-16149; только-B — L1-13706, L1-14546, L1-16256 (`produces` — 0 факто-игл, точка про общие знания, B угадал — A нет). Остаточный both-fail 11 = 2 новых (L1-12953, L1-14975) + дыры §4.1 (L1-13165, L1-13716, L1-14563) + ретрив-фейлы §4.2 (L1-13636, L1-14494, L1-14506, L1-14931) + перемаркированные парафразы (L1-14925, L1-16116). Гигиена убрала шум разметки; звенья write-time/read-time остались — таргеты последующих правок 1–2.

## 3. Разбор both-fail (17 точек)

Метод: послойная трассировка must-токена — сырое сообщение (source) → факт (evidence vs M) → M-слепок → ответ плеча. Классификация:

| слой | число | точки | суть |
|---|---|---|---|
| неисполнимые (разметка) | 6 | L1-12725 change-password, L1-12912 allowedOriginPatterns, L1-13583 «девопсов», L1-14596 types-with-counts, L1-14861 наглядность, L1-16205 ApiNotFoundController | токен входит в корпус только в trigger/truth — в M-слепок непропускаем by construction; протокол 33 не проверял вхождение до M |
| парафраз-тир (оракул) | 4 | L1-16256 «аннотацией» (×40 в сырце), L1-16116 «переиспользовать» (×26), L1-14404 «словарь» (×6), L1-14925 «кусочка экрана» | знание в корпусе есть, но must = дословный токен юзера; word-boundary оракул для парафраза недостижим → ярус судьи (вне smoke-яруса) |
| дыры экстракции (write-time) | 3 | L1-13165 @Transient, L1-13716 updateTs, L1-14563 basic | §4.1 |
| фейлы ретрива (read-time) | 4 | L1-13636 192.0.2.21, L1-14494 typeId, L1-14506 kind, L1-14931 initial | §4.2 |

Итог: таргеты качества конвейера — **7 из 17**; 10 — шум датасета/оракула (на 28 точках это граница шума метрики ±10 pp).

## 4. Диагноз звеньев

### 4.1. Write-time: экстракция

Три разные моды отказа одного батч-экстрактора (промпт `ExtractionPrompt.SYSTEM`, 20 реплик / 2000 токенов):

1. **Недоизвлечение плотных батчей** — L1-13165: msg 12933 (буллет-список «Ключевые факты») дал 2 факта; @Transient-буллет не выдан — токен отсутствует во всех сырых ответах экстрактора за ран. Rule 10 («Prefer 0 facts over noise») консервативна; факт материализовался только из truth-реплики (13169).
2. **Потеря идентификаторов в парафразе** — L1-13716: из msg 12765 извлечено 7 фактов, включая «audit fields are redundant» — statement без перечня полей (createdBy/…/updateTs): семантика есть, дискретный токен слит в обобщение.
3. **Поздняя материализация** — L1-14563: «basic» в сырце ×8 до M; факт сформулирован только из 14571 (> M).

Побочные дефекты: факт с пустым statement (57ab727c, evidence [12715, 12719, 12765]); дедуп-конфликты — 120 журнальных записей за ран (конфликт = журнал + INSERT отдельной строкой, факты не теряются — DedupPipeline), но 25 конфликтов на одном якоре `(user, code_style_preference)` = зоопарк синонимичных объектов → разбухание constructs.

### 4.2. Read-time: digest без ретрива

Digest плеча A — **полный слепок** плоским списком (`Arms.memoryDigest`: секции critical → constructs → preferences, факт = statement/SPO); сортировка SnapshotBuilder — reinforcement DESC, createdAt; **селекции по релевантности к триггеру нет**.

| точка | фактов в слепке | must-игла | позиция иглы | промпт A |
|---|---|---|---|---|
| L1-13636 | 489 | 192.0.2.21 ×3 | 360–383 / 428 | 18.8K ток (ответ — 31 токен) |
| L1-14494 | 763 | typeId ×12 | 41 / 681 | 26.4K |
| L1-14506 | 771 | kind ×17 | 351 / 689 | 26.2K |
| L1-14931 | 917 | initial ×5 | 65 / 823 | 32.7K |

Игла не поднимается даже с ранней позиции (41/681) — needle-in-a-haystack на 500–900 строках. При этом `facts.embedding` и `facts.w` материализованы, но в компоновке digest не участвуют.

## 5. Заключение по ассерту и правки

Ассерт тезис-чека (`lift > 0`, спека 28) **не выполнен** (−4 pp, p = 1.0) — условный no-go в терминах §4.2 спеки 28: правка диагностированного звена до следующего рана. Диагноз: не «память бесполезна», а два отсутствующих зуба конвейера — write-time гарантии деталей и read-time селекция; метрика при этом сама шумная (10/17 both-fail — шум).

Предлагаемые правки до следующего рана (решение за владельцем):

1. **(write)** Правило verbatim-идентификаторов в промпт экстракции (имена полей, аннотации, IP, пути — сохранять дословно); опция two-pass (черновой список → фильтр); фикс пустых statement при вставке.
2. **(read)** Top-K селекция digest по embedding-похожести триггер↔факт (embeddings уже материализованы); w/reinforcement — tie-break.
3. **(датасет) — выполнено 19.09 (§2.1)**: выбраковано 7, добавлено 4 (И2+И4 проверены), 3 перемаркировано; проверка вхождения must-токена в корпус до M — кандидат в протокол 33; парафраз-точки — ярус судьи (полный ярус, вне smoke).
4. **(дедуп)** Словарь объектов для якорей предпочтений (зоопарк `code_style_preference`).

## 6. Экономика и воспроизводимость

* Полный цикл (replay → wcheck → arms → oracles → report) — ~40 мин, ~1.3M prompt / ~144K completion токенов; replay 34.4 мин (187 вызовов), arms ~7 мин (56 вызовов).
* Повторные прогоны дешевеют: кэш ответов (`point_fp`), чекпоинты инжеста, слепки/факты переиспользуются; смена промпта экстракции — новый эксперимент по §7.2.
* Артефакты: эксперимент `project-v3` (active) в `gestalt_eval`, runs #220–231 (№225 — диагностический отказ стража до ввода проекции), слепки 48 (40 original + 8 проекцией), факты 1615; отчёт — `eval/out/report.md` (стираемый дамп, канон — PG).
