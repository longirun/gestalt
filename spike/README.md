# Спайк ядра (Р34)

> **Статус:** исследовательский спайк **завершён** (2026-09-07).  
> Код законсервирован и сохраняется в репозитории до архивации при переходе к реализации ядра по ТЗ (**Р34**).  
> Итоги эмпирической верификации, доказательства реализуемости и границы валидности — **[`../docs/research/25 - Эмпирическая верификация ядра концепции.md`](../docs/research/25%20-%20Эмпирическая%20верификация%20ядра%20концепции.md)**.

Вертикальный срез write-path: `Ingest → Encoding → Pure PG → PortraitSnapshot`.  
Задание, границы и критерии приёмки — **[`../docs/spec/24 - ADR спайка ядра (Р34).md`](../docs/spec/24%20-%20ADR%20спайка%20ядра%20%28Р34%29.md)**.

## Запуск

```bash
cp spike/local.properties.example spike/local.properties   # заполнить креды (файл в .gitignore)
./gradlew -p spike test                                     # детерминированное ядро
./gradlew -p spike run -Pargs=fixture-batch                 # синтетика: батчинг без БД и LLM
./gradlew -p spike run -Pargs=all                           # полный прогон (нужны БД + LLM)
```

Требования: JDK 21, PG-инстанция :5433 (`gestalt_spike` — запись, `honcho_memory` — только чтение), ключ OpenAI-совместимого LLM-роутера.

## Гигиена

Коммитятся только код, SQL, промт и синтетический фикстур. `local.properties`, `data/`, `out/` — вне VCS.
