# Gestalt — 17: Открытые вопросы модели Fact (WORLD) -черновик

> 2026-09-05 · Источник: ревью канона `16 - Fact и домен WORLD` после дефрагментации · Статус: **черновик в работе**.
> Назначение: проработка открытых вопросов док 16 **по одному за сессию** (порядок П1→П4), без каши. Принятое — правкой канона 16 и строкой в реестре (STATUS).
> Уже принято и внесено в 16 в сессии ревью: виды фактов STATE/NARRATIVE/EVENT и гибридная обязательность anchor (16 §2.2); инварианты горячего набора (16 §3.1). Нить — Н12 (STATUS).
> 2026-09-05 (сессия identity): **П1 закрыт → Р23** (канон 16 §2.3/§4.1). **П2 пересмотрен (поздняя сессия):** «типизированное ядро» отвергнуто — словарь эмерджентный, write-time feedback, канонизаторы форм, единая консолидация (16 §2.2 п.4, §2.3; Р23).
> 2026-09-05 (финальная сессия): **П2-остаток закрыт → Р25** (канон 16 §2.2 п.4–5, §2.3). Эмпирический референс — обезличенный аудит живой рабочей базы (файл удалён, не публикуется). Далее — П3.

---

## П1. Identity факта и dedup-key — ✅ закрыт (2026-09-05) → Р23

Принята развилка (в) с уточнениями сессии: identity = `(owner, scope, project_id, subject_norm, predicate_norm, conditions)`, только STATE; `object` — версииемое значение; инвариант ≤1 ACTIVE на identity; двухшаговый детерминированный дедуп; `conditions` → sibling (развилка F1); custom-предикаты — та же механика (E). Канон: 16 §2.3, 16 §4.1. Источники: Кинч и ван Дейк, Кодд/FD, ACT-R, MV-register (CRDT).

---

## П2. Словарь предикатов anchor — ✅ закрыт полностью (2026-09-05) → Р25

### Постановка

Identity из П1 опирается на `(subject, predicate)`. Если экстрактор генерирует предикаты свободно,
один смысл получает несколько ключей: `runtime.version`, `java.version`, `stack.java` — три разных
identity для одного свойства. Цепочки дедупа и supersession рвутся. Это **predicate sprawl** —
точный аналог axis sprawl из Н4, только в домене WORLD.

Асимметрия с Н4 принципиальна: для PSYCHE идиографичность обязательна (оси навязывать нельзя, Р4);
для WORLD номотетика легитимна — мир общий, свойства мира типизируемы, как единицы измерения.
Закрытый словарь предикатов не нарушает концепцию, а обслуживает её.

### Варианты

| Вариант | Суть | Плюсы | Минусы |
|---|---|---|---|
| (1) закрытый мини-словарь + escape-hatch | типизированный набор v1 (version, port, path, dependency, name, endpoint, status, decision, agreement, …) + `custom` для остального (неканонический: в identity-дедупе не участвует, ждёт канонизации) | детерминированность из коробки; расширение — миграция, не рантайм-свобода; дёшево | словарь надо спроектировать; «не хватает предиката» — лечится custom |
| (2) свободный словарь + merge | предикаты свободные; синонимы сливаются по косинусу (порог ~0.85, как оси в Н4) | гибкость; знакомый механизм | та же борьба со sprawl, что и в Н4; merge-шум в рантайме |
| (3) гибрид | закрытое ядро + свободный хвост; канонизация хвоста в Dreaming | баланс | два механизма вместо одного; фоновая нагрузка канонизации |

### Принято (2026-09-05, первый заход — заменено пересмотром ниже)

Принцип типизированного ядра (основа — вариант 1): канонический предикат несёт тип значения + каноническую форму + семантику сравнения; нормализация object — атрибут типа предиката. → 16 §2.2. *(Историческая запись; см. «Пересмотр».)*

### Пересмотр (2026-09-05, поздняя сессия)

Отвергнуто: фиксированный seed из golden-датасета (доменная предвзятость — система обязана работать в любом домене с первого дня; golden — только фикстура метрики Р14, не контент продукта) и «словарь как проектируемый артефакт» (10–15 предикатов не стоят проектирования — словарь выращивается). Сняты вопросы: фиксированный/динамический хвост (хвоста нет — один эмерджентный словарь), токен-бюджет (feedback ~50–100 токенов на батч, амортизация Р8; дедуп — SQL, словарь не читает), «merge в Dreaming как рутина» (LLM — только judge над редкими парами; конвергенция и кластеризация детерминированные).

Осталось (✅ закрыто финальной сессией 2026-09-05 → **Р25**, канон 16 §2.2 п.4–5, §2.3):

1. **Состав feedback-промпта:** словарь **кладётся** (развилка «класть ли» закрыта — эмпирика
   насыщения подтверждает жизнеспособность). Три пространства: предикаты + subject-имена +
   conditions-теги (все входят в identity). Отбор: top-N по использованию + «ясли» (слоты для
   неподтверждённых новых имён — лечение rich-get-richer). Мягкая формулировка предпочтения +
   naming-политика формы (детерминированный regex-валидатор). «Живое» имя = ≥1 ACTIVE-факт.
   Словарь — проекция над `facts`, не хранимая сущность.
2. **Порог кандидата консолидации:** сигнальный каскад (кандидат = любой сигнал): pg_trgm
   (литеральный) → fingerprint ко-использования (общий subject_norm + близкий object —
   сильнейший, чистый SQL) → косинус pgvector, дефолт **0.85** (верифицирован внутренним
   аудитом живой базы: −53% дублей против −22% у 0.90; привязка к embedding-модели — конфиг).
3. **Регламент judge-батча:** контур **Reconciliation** (техгигиена, не Dreaming); лимиты по
   времени и объёму; drain-режим (backoff сжимается при остатке очереди); вход судьи = пара +
   мини-конкорданс (2–3 примера фактов с каждой стороны); каноническое имя — тай-брейк
   (использование → возраст → лексикография); merge-миграция коллизии ACTIVE разрешает обычным
   дедупом §4.1; идемпотентность и провенанс — таблица `dictionary_pairs` с
   UNIQUE(namespace, a, b) (единственная новая сущность); poison-карантин по Р10.
4. **Канонизаторы форм v1:** количество (число+единица) / версия (кортеж, нули, квалификатор-
   класс) / путь / url / строка; каскад по форме: url → путь → версия → количество → строка;
   число↔версия сравнимы по числовому ядру; принцип консервативности — сливать только заведомо
   равное, сомнительное → судье.

Не включено (резерв): rename-канал (вырожденный merge 1→1 для явного переименования имени) —
отложено; всплывёт по факту шума нейминга после стабилизации словарей.

Эмпирика (обезличено): порог 0.85 и насыщение словарей верифицированы аудитом живой рабочей
базы (~20k сообщений, bge-m3/pgvector); порядок величин — референс для NFR/ТЗ, не канон
(доменная нейтральность: доли WORLD/PSYCHE/Ephemeral в других доменах дрейфуют).

### Критерий закрытия

✅ Выполнено: решения внесены в 16 §2.2 п.4–5 и §2.3; числа (N, пороги сигналов, K, каденции,
батчи) — дефолты конфига, выносятся в ТЗ (Н8). Строка реестра — **Р25**.

---

## П3. Доступность RETIRED-истории после Generalization (§4.3 vs урок HippoGraph)

### Постановка

В каноне два утверждения, которые нужно развести:

1. §4.3: после синтеза сводного факта («стек тестирования: JUnit 5, AssertJ, Testcontainers»)
   первичные атомарные факты получают `RETIRED` — «предотвращая засорение контекста фрагментами»;
2. Р6 (урок HippoGraph): supersession меняет статус, **но не подавляет retrieval** — старые факты
   остаются reasoning-контекстом («на чём мы писали до марта 2026?»).

С инвариантом горячего набора (16 §3.1: поисковые индексы — только ACTIVE) буквальное прочтение даёт:
RETIRED-атомы **вообще выпадают из любого поиска**. Вопрос «какие библиотеки мы подключали за год?»
останется без ответа из фактов — останется только сырая эпизодика (Message-логи).

### Варианты

| Вариант | Суть | Плюсы | Минусы |
|---|---|---|---|
| (а) история недоступна из фактов | RETIRED — вне поиска; ответы о прошлом — только по Message-логам | максимально чистый горячий набор | противоречит духу Р6; теряем дешёвый доступ к истории |
| (б) exact-доступ к истории | RETIRED вне **семантического** горячего набора, но: полный (не-partial) служебный индекс по anchor для exact-запросов + подъём RETIRED при явном временном намерении запроса (temporal query-side, Н5) | история достижима дёшево и детерминированно; горячий набор не растёт; совместимо с §3.1 (служебные индексы — полные) | temporal query-side — зависимость от Н5 |
| (в) не RETIRED, а штраф ранга | атомарные факты не ретайрить, а понижать в ранжировании (ниже сводного) | ничего не теряем | RETIRE-семантика размывается; атомы продолжают шумить в семантическом поиске |

### Рекомендация

**(б)**: RETIRED = вне семантического горячего набора, но достижим exact-запросом по anchor и поднимается
при временном намерении запроса. В 16 §4.3 добавляется одна строка о доступности истории.

### Критерий закрытия

§4.3 канона дополнен семантикой доступности RETIRED; при необходимости — уточнение §3.1 (состав полных индексов).

---

## П4. Принцип вычислимой важности

### Постановка

Формула ранжирования §5.2 уже вычисляет «важность» на read-path:

$$\text{Final\_Score} = \text{RRF (релевантность запроса)} \times \text{decay (свежесть)} \times (1 + 0.2 \cdot \ln(1 + \text{reinforcement}))$$

Хранимое на факте — только **политики**: `is_critical` (доставка в слепок) и `permanence` (иммунитет
от TTL/decay). Числа-«приоритета» на факте нет — и это принципиально:

- важность без запроса не существует: «порт базы 5432» — топ-1 при вопросе о подключении
  и шум при вопросе о стиле кода;
- reinforcement — множитель, не пропуск: 40 подтверждений не продавливают порог no-false-memory (Р14),
  если факт не релевантен текущему запросу;
- хранимый приоритет — известный антипаттерн (NGT, 14 §2: тег «важное» на факте про настолки вытеснил
  факт об инвалидности — хранимая метрика не знает текущего запроса).

### Развилка

| Вариант | Суть | Плюсы | Минусы |
|---|---|---|---|
| (а) зафиксировать явно | строка-принцип в 16 §5: «важность — вычислимая функция read-path, не атрибут факта; хранимое — только политики доставки/жизни» | страховка от будущих score-полей; явно для писца ТЗ (Н8) | формально дублирует то, что уже говорит формула |
| (б) оставить как есть | формула §5.2 говорит сама | ноль изменений | принцип не назван — риск эрозии (соблазн добавить stored-priority при локальной оптимизации) |

### Рекомендация

**(а)**: одна строка-принцип + пример в §5. Стоимость нулевая, защита от эрозии реальная.

### Критерий закрытия

Принцип внесён в 16 §5 (или мотивированный отказ — зафиксировать здесь).

---

## Порядок проработки

П1 ✅ → П2 ✅ (Р25) → П3 → П4. Каждый пункт — отдельным заходом; решение сразу в канон 16 + реестр (STATUS).
